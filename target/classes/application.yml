spring:
  application:
    name: probationwork
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mydata?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf-8
    username: root
    password: djx55707
  mail:
    host: smtp.qq.com # 邮箱服务器主机地址
    username: 2974651270@qq.com # 你的邮箱地址
    password: fxlzsuihiriudfjc # 授权�?
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            enable: true
  servlet:
    multipart:
          max-file-size: 10MB  # 单个文件最大大�?
          max-request-size: 100MB  # 整个请求最大大�?
server:
  port: 8080

logging:
  file:
    name: work.log


save:
  path: D:/Java/JavaProject/probationwork/src/main/resources/static/images/

base:
  url: /images/
